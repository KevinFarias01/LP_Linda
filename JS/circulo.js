function corCirculo(cor) {
    const circulo = document.querySelector('.circulo');
    circulo.style.background = cor;
}

function tamCirculo(tam) {
    const tamanhoDoCirculo = document.querySelector('.circulo');
    circulo.style.clipPath = "circle(600px at right 700px)";
}